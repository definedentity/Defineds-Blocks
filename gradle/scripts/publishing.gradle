modrinth {
    token = System.getenv("MODRINTH_TOKEN")
    projectId = "6AYENgHJ"
    versionNumber = mod_version
    versionType = "release"
    uploadFile = remapJar
    gameVersions = ["1.18.2"]
    loaders = ["fabric"]
    dependencies {
        required.project "syUFsyMr"
    }
}